---
// Different images for each row - using proper Astro image imports
const topRowImages = [
  '/src/assets/LOGOS/logoblancinegredrakaris.png',
  '/src/assets/LOGOS/dks.png', 
  '/src/assets/gallery/foto-3.jpg',
  '/src/assets/gallery/foto-4.jpg',
  '/src/assets/gallery/foto-5.jpg',
  '/src/assets/gallery/foto-6.jpg'
];

const bottomRowImages = [
  '/src/assets/gallery/foto-7.jpg',
  '/src/assets/gallery/foto-8.jpg',
  '/src/assets/gallery/foto-9.jpg', 
  '/src/assets/gallery/foto-10.jpg',
  '/src/assets/gallery/foto-11.jpg',
  '/src/assets/gallery/foto-12.jpg'
];
---

<section class="gallery-section">
  <!-- Gallery title -->
  <div class="gallery-header">
    <h2>GALERÍA</h2>
  </div>
  
  <!-- Photo gallery -->
  <div class="photo-gallery">
    <!-- First row - moves left -->
    <div class="photo-row photo-row-left">
      <div class="photo-track">
        {[...topRowImages, ...topRowImages, ...topRowImages].map((image, index) => (
          <div class="photo-item">
            <img src={image} alt={`Foto ${index + 1}`} />
            <div class="photo-placeholder">FOTOS</div>
          </div>
        ))}
      </div>
    </div>
    
    <!-- Second row - moves right -->
    <div class="photo-row photo-row-right">
      <div class="photo-track">
        {[...bottomRowImages, ...bottomRowImages, ...bottomRowImages].map((image, index) => (
          <div class="photo-item">
            <img src={image} alt={`Foto ${index + 1}`} />
            <div class="photo-placeholder">FOTOS</div>
          </div>
        ))}
      </div>
    </div>
  </div>
</section>

<style>
  .gallery-section {
    position: relative;
    background-color: var(--white-background);
    padding: 80px 0;
    overflow: hidden;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    min-height: 80vh;
  }

  .gallery-section::before {
    content: '';
    position: absolute;
    top: -120px;
    left: 0;
    width: 100%;
    height: 120px;
    background: var(--background-color);
    transform: skewY(2deg);
    transform-origin: top left;
    z-index: 1;
  }

  .gallery-section::after {
    content: '';
    position: absolute;
    bottom: -120px;
    left: 0;
    width: 100%;
    height: 120px;
    background: var(--background-color);
    transform: skewY(-2deg);
    transform-origin: bottom left;
    z-index: 1;
  }
  
  .gallery-header {
    text-align: center;
    padding: 4rem 0 2rem 0;
    position: relative;
    z-index: 2;
  }
  
  .gallery-header h2 {
    font-size: 2.5rem;
    font-weight: bold;
    color: var(--title-color);
    letter-spacing: 2px;
    margin: 0;
  }
  
  .photo-gallery {
    padding: 2rem 0;
    position: relative;
    z-index: 2;
    width: 100%;
  }
  
  .photo-row {
    overflow: hidden;
    white-space: nowrap;
    margin-bottom: 2rem;
  }
  
  .photo-track {
    display: inline-flex;
    animation-duration: 60s;
    animation-timing-function: linear;
    animation-iteration-count: infinite;
    will-change: transform;
  }
  
  .photo-row-left .photo-track {
    animation-name: scrollLeftSmooth;
  }
  
  .photo-row-right .photo-track {
    animation-name: scrollRightSmooth;
  }
  
  .photo-item {
    position: relative;
    width: 300px;
    height: 200px;
    margin-right: 20px;
    flex-shrink: 0;
    overflow: hidden;
    background: #a08a8a;
    border-radius: 8px;
  }
  
  .photo-item img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }
  
  .photo-placeholder {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    font-size: 1.5rem;
    font-weight: bold;
    color: #333;
    text-shadow: 2px 2px 4px rgba(255,255,255,0.8);
    pointer-events: none;
  }
  
  @keyframes scrollLeftSmooth {
    from {
      transform: translateX(0);
    }
    to {
      transform: translateX(calc(-100% / 3));
    }
  }
  
  @keyframes scrollRightSmooth {
    from {
      transform: translateX(calc(-100% / 3));
    }
    to {
      transform: translateX(0);
    }
  }
  
  @media (max-width: 768px) {
    .gallery-header h2 {
      font-size: 2rem;
    }
    
    .photo-item {
      width: 200px;
      height: 150px;
      margin-right: 15px;
    }
    
    .photo-track {
      animation-duration: 45s;
    }
  }
  
  @media (max-width: 480px) {
    .photo-item {
      width: 150px;
      height: 112px;
      margin-right: 10px;
    }
    
    .photo-track {
      animation-duration: 35s;
    }
    
    .photo-placeholder {
      font-size: 1rem;
    }
  }
</style>
